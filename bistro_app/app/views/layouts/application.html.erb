<!DOCTYPE html>
<html>
<head>
  <title>BistroApp</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <h1> Gadoe's Bistro! </h1>
  <div class="login">
    <% if session[:current_user_id] %>
    <h2>Hello! Nice to see you</h2>
    <%= link_to "#{User.find(session[:current_user_id]).username}", user_path(session[:current_user_id]) %> ||
    <%= link_to "Logout", sessions_path, method: :delete %>
    <% else %>
    <p>Are You Already A Memeber? Please Sign In!</p>
    <%= form_tag(sessions_path) do%>
    <%= label_tag :username %>:
    <%= text_field_tag :username %>
    <br>
    <%= label_tag :password %>:
    <%= password_field_tag :password %>
    <br><br>
    <%= submit_tag "Login"%>
    <% end %>
    <% end %>
  </div>

<!-- <a href="/users/new"> Path! </a> -->
 <%= link_to "Sign up Here!", new_user_url , method: :get %>
 <p>stuff after this is in yield: </p>
 <%= link_to "Home", root_path %>
<%= yield %>


</body>
</html>
